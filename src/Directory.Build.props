<Project>
  <PropertyGroup>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>

  <Target Name="FormatDocument" BeforeTargets="Build">
    <Exec Command="dotnet format" />
  </Target>

</Project>