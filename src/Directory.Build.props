<Project>
  <PropertyGroup>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>

  <Target Name="FormatDocument" BeforeTargets="Rebuild">
    <Exec Command="dotnet format" />
  </Target>

</Project>